# Wordlish 設計書

## ゲーム仕様

### 基本ルール

- 5文字の英単語を当てる
- 推測は6回まで
- 各推測後にフィードバックを表示

### 判定ロジック

| 状態 | 意味 |
|------|------|
| 緑 (Correct) | 文字・位置ともに正解 |
| 黄 (Present) | 文字は含まれるが位置が違う |
| 灰 (Absent) | 文字は含まれない |

### 重複文字の判定アルゴリズム

これが最も複雑な部分。2パスで処理する：

```
入力: guess = "ABATE", answer = "APART"

Pass 1 - 完全一致（緑）を先に処理:
  A[0] vs A[0]: 緑 → answerから消費
  B[1] vs P[1]: 保留
  A[2] vs A[2]: 緑 → answerから消費
  T[3] vs R[3]: 保留
  E[4] vs T[4]: 保留

  answer残り: [P, R, T]

Pass 2 - 残りを判定:
  B[1]: 灰 (残りにBなし)
  T[3]: 黄 (残りにTあり) → answerから消費
  E[4]: 灰 (残りにEなし)

結果: [緑, 灰, 緑, 黄, 灰]
```

ポイント：推測内の重複文字は、正解に含まれる回数分だけ緑/黄になり、超過分は灰になる。

### 単語リスト

| 種類 | 用途 | 備考 |
|------|------|------|
| 正解リスト | 出題対象 | 約2,300語、一般的な5文字英単語 |
| 推測許可リスト | 入力検証 | 約13,000語、正解リストを含む |

固有名詞、侮辱語は除外。

---

## 技術設計

### ターゲット

Erlang (BEAM VM)

理由：
- 標準入力の同期読み取りが容易
- CLIに適したブロッキングI/O

### 依存パッケージ

```toml
[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
stdin = ">= 2.0.0 and < 3.0.0"
gleam_community_ansi = ">= 1.4.0 and < 2.0.0"
```

- `stdin`: 標準入力読み取り
- `gleam_community_ansi`: 色付きターミナル出力

### モジュール構成

```
src/
  wordlish.gleam      # エントリポイント、ゲームループ
  wordlish/
    game.gleam        # ゲームロジック（状態管理、勝敗判定）
    judge.gleam       # 推測判定アルゴリズム
    words.gleam       # 単語リスト、ランダム選択、検証
    ui.gleam          # ターミナル表示（色付き出力）
```

### データ型

```gleam
// 各文字の判定結果
pub type LetterResult {
  Correct   // 緑: 位置も文字も正解
  Present   // 黄: 文字は含まれるが位置違い
  Absent    // 灰: 文字は含まれない
}

// 1回の推測結果
pub type GuessResult {
  GuessResult(letters: List(#(String, LetterResult)))
}

// ゲーム状態
pub type GameState {
  GameState(
    answer: String,
    guesses: List(GuessResult),
    max_attempts: Int,
  )
}

// ゲーム終了状態
pub type GameOutcome {
  Won(attempts: Int)
  Lost(answer: String)
  InProgress
}
```

### ゲームフロー

```
1. 正解単語をランダム選択
2. ループ開始（最大6回）
   2.1. プロンプト表示、入力待ち
   2.2. 入力検証（5文字か？辞書にあるか？）
   2.3. 判定実行
   2.4. 結果表示（色付き）
   2.5. 勝利判定 → 終了 or 継続
3. 結果表示（勝ち/負け）
```

### UI仕様

```
Wordlish - 5文字の単語を当てよ (6回まで)

> apple
A P P L E
🟩🟨⬜⬜🟩

> ...

正解！ 3回で当てた
```

色表現：
- 緑: `ansi.bg_green` + `ansi.black`
- 黄: `ansi.bg_yellow` + `ansi.black`
- 灰: `ansi.bg_bright_black` + `ansi.white`

---

## 実装順序

1. `judge.gleam` - 判定アルゴリズム（TDDで実装）
2. `words.gleam` - 単語リスト管理
3. `ui.gleam` - 表示機能
4. `game.gleam` - ゲーム状態管理
5. `wordlish.gleam` - メインループ統合
